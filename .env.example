# Meta Ads Analyzer - Configuration
# Copiez ce fichier en .env et remplissez vos valeurs

# ═══════════════════════════════════════════════════════════════════════════════
# Meta API Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# Token d'accès Meta Ads API (obligatoire)
# Obtenez-le sur https://developers.facebook.com/
META_ACCESS_TOKEN=votre_token_meta_ici

# ═══════════════════════════════════════════════════════════════════════════════
# Base de données PostgreSQL
# ═══════════════════════════════════════════════════════════════════════════════

# URL de connexion complète (utilisée par l'application)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/meta_ads

# OU configuration séparée (utilisée par docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=meta_ads
POSTGRES_PORT=5432

# ═══════════════════════════════════════════════════════════════════════════════
# Application
# ═══════════════════════════════════════════════════════════════════════════════

# Port de l'application Streamlit (Railway définit automatiquement PORT)
APP_PORT=8501

# ═══════════════════════════════════════════════════════════════════════════════
# Proxy (optionnel - pour éviter les bans lors du scraping)
# ═══════════════════════════════════════════════════════════════════════════════

# ScraperAPI - Proxy résidentiel avec rotation automatique
# Obtenez une clé gratuite sur https://www.scraperapi.com/ (1000 req/mois gratuit)
# Si non défini, les requêtes seront directes (risque de ban plus élevé)
SCRAPER_API_KEY=

# ═══════════════════════════════════════════════════════════════════════════════
# Proxy SSL Configuration (MarketSpy V2)
# ═══════════════════════════════════════════════════════════════════════════════

# Chemin vers le certificat CA du proxy (optionnel)
# Si défini et valide, les requêtes utiliseront ce certificat pour la vérification SSL
# Si non défini ou invalide, fallback sur verify=False (moins sécurisé mais fonctionnel)
# Exemple: /path/to/proxy-ca-bundle.crt
PROXY_CA_BUNDLE=

# ═══════════════════════════════════════════════════════════════════════════════
# Railway Deployment
# ═══════════════════════════════════════════════════════════════════════════════
# Sur Railway, configurez ces variables dans le dashboard :
#
# 1. META_ACCESS_TOKEN  - Votre token Meta API
# 2. DATABASE_URL       - Fourni automatiquement par Railway PostgreSQL
# 3. PORT               - Défini automatiquement par Railway
# 4. SCRAPER_API_KEY    - (Optionnel) Clé ScraperAPI pour le proxy
#
# Architecture recommandée sur Railway :
# - Service "web"    : Application Streamlit (Procfile: web)
# - Service "worker" : Scheduler automatique (Procfile: worker)
# - Add-on PostgreSQL: Base de données partagée entre les deux services
